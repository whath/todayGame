# V0.1 待执行验收

**以下为 v0.1 首次交付时的实机验收清单，仍未在 Creator / Windows 实际运行。v0.2 的自动验证和最新操作步骤单独记录在 VALIDATION_V0.2.md；不能用逻辑测试替代此清单的实机结果。**

| 项目 | 后续操作 / 预期 | 当前结果 |
| --- | --- | --- |
| 工程导入 | Creator 3.8.6 导入，Scene / Prefab / 脚本引用完整 | NOT RUN |
| Windows 启动 | 原生构建启动至加入界面 | NOT RUN |
| 双键盘 | WASD 组先加入、方向键组后加入，双方独立移动；交换加入顺序 | NOT RUN |
| 键盘 + 手柄 | 键盘先加入 P1，手柄后加入 P2 | NOT RUN |
| 手柄 + 键盘 | 手柄先加入 P1，键盘后加入 P2 | NOT RUN |
| 双手柄 | 两个物理手柄按顺序加入，同时移动 | NOT RUN |
| 独占分配 | 已绑定设备再按键，不占用另一个槽位 | NOT RUN |
| 模拟输入 | 轻推慢移，死区静止，斜向不超速，十字键正确 | NOT RUN |
| 抽象动作 | 面键 / FGE / JKL 显示对应标签，按住不重复触发 | NOT RUN |
| 共享镜头 | 靠近、远离、向相反角落移动，取景与平滑正确 | NOT RUN |
| 碰撞 | 四面接近障碍、贴角、贴墙、同时移动，不穿静态障碍 | NOT RUN |
| 断开 | 拔掉当前手柄，界面提示，双方暂停，槽位保留 | NOT RUN |
| 重连 | 插回并按按钮，恢复原待接管槽位，另一玩家不受影响 | NOT RUN |
| 双断开 | 同时断开两只手柄，按 P1 / P2 顺序重新加入 | NOT RUN |
| 替代设备 | 断开后用空闲键盘映射接管，不抢走已绑定输入源 | NOT RUN |
| 已连接设备 | 启动前插好手柄，启动后按按钮能加入 | NOT RUN |
| 失焦 | 按住移动后切出窗口并松开，返回无卡键 / 大幅跃迁 | NOT RUN |
| Reset / Lobby | F5 重置位置且保留设备，设置页确认返回加入界面后释放槽位 | NOT RUN |
| 生命周期 | 重进场景 / 禁用再启用，无重复监听和双倍输入 | NOT RUN |
| Console | 无 Error，记录真实警告和错误来源 | NOT RUN |
| Windows Build | 构建成功且产物内四种组合可操作 | NOT RUN |
| 文档 | 与实际验证后的行为保持一致 | NOT RUN |

执行时记录：Creator 版本、Windows 版本、手柄型号 / 连接方式、窗口尺寸、操作步骤、实际结果与必要截图。失败条目使用 BUG_TEMPLATE，不把浏览器成功当作 Windows 原生成功。
